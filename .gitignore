# Local tooling / editor config (machine-specific)
.claude/

# Dependencies — none are committed; the site is zero-dependency
node_modules/

# Python tooling cruft (tools/make_covers.py etc.)
__pycache__/
*.pyc

# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# The nightly wildfire refresh checks the model repo out here (see
# .github/workflows/wildfire-refresh.yml). It is a whole second repo; never commit it.
.wildfire/
